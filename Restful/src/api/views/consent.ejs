<!DOCTYPE html>
<html lang="pt-br">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Consentimento</title>

        <link rel="stylesheet" href="/assets/css/auth.css" />
    </head>
    <body>
        <div class="container">
            <h1>Consentimento</h1>

            <p class="subtitle">
                O aplicativo <strong><%= clientName %></strong> solicita acesso
                a:
            </p>

            <ul class="scope-list">
                <li>Email</li>
                <li>Nome de usuário</li>
            </ul>

            <div class="actions">
                <form
                    method="post"
                    action="/interaction/<%= uid %>/consent/confirm"
                >
                    <button type="submit" class="primary">Permitir</button>
                </form>

                <form
                    method="post"
                    action="/interaction/<%= uid %>/consent/abort"
                >
                    <button type="submit" class="secondary">Cancelar</button>
                </form>
            </div>
        </div>
    </body>
</html>
